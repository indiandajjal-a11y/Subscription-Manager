{
  "name": "subscription-manager",
  "version": "5.3.0",
  "description": "ISP Subscription Manager: TMF-aligned subscription lifecycle management with SonarQube quality integration.",
  "type": "module",
  "scripts": {
    "start": "node code/server.js",
    "test": "node --test testing/*.test.js",
    "coverage": "node --test --experimental-test-coverage --test-reporter=lcov --test-reporter-destination=coverage/lcov.info testing/*.test.js"
  },
  "dependencies": {
    "pg": "^8.16.0"
  },
  "engines": {
    "node": ">=20"
  }
}
